import { Account, CreateAccountDto, UpdateAccountDto } from '.';

// eslint-disable-next-line
export type AccountIndividual = Account & {};

// eslint-disable-next-line
export type CreateAccountIndividualDto = CreateAccountDto & {};

// eslint-disable-next-line
export type UpdateAccountIndividualDto = UpdateAccountDto & {};
