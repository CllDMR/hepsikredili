export type Plan = { name: string; price: number; adLimit: number };

export type CreatePlanDto = { name: string; price: number; adLimit: number };

// eslint-disable-next-line
export type UpdatePlanDto = {};
