import { AdDetailSatilikDaire } from './satilik-daire.schema';

describe('AdDetailSatilikDaire Data-access', () => {
  it('should be defined', () => {
    expect(new AdDetailSatilikDaire()).toBeDefined();
  });
});
