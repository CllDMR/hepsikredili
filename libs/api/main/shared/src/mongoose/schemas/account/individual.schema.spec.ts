import { AccountIndividual } from './individual.schema';

describe('AccountIndividual Data-access', () => {
  it('should be defined', () => {
    expect(new AccountIndividual()).toBeDefined();
  });
});
