import { ApiMainCloudinaryModule } from '@hepsikredili/api/main/cloudinary';
import {
  ApiMainSharedCaslModule,
  ApiMainSharedMongooseModule,
} from '@hepsikredili/api/main/shared';
import { ConfigModule, ConfigService } from '@nestjs/config';
import { MongooseModule } from '@nestjs/mongoose';
import { Test, TestingModule } from '@nestjs/testing';
import { ThrottlerModule } from '@nestjs/throttler';
import { ApiMainAccountImageService } from '../services/account-image.service';
import { ApiMainAccountImageController } from './account-image.controller';

describe('ApiMainAccountImageController', () => {
  let controller: ApiMainAccountImageController;

  beforeEach(async () => {
    const module: TestingModule = await Test.createTestingModule({
      imports: [
        ConfigModule.forRoot({
          isGlobal: true,
          expandVariables: true,
          cache: true,
          envFilePath: `${process.cwd()}/apps/api/main/.env`,
        }),
        ThrottlerModule.forRootAsync({
          imports: [ConfigModule],
          inject: [ConfigService],
          useFactory: async (config: ConfigService) => ({
            ttl: config.get('RATE_LIMIT_TTL'),
            limit: config.get('RATE_LIMIT_LIMIT'),
          }),
        }),
        MongooseModule.forRootAsync({
          imports: [ConfigModule],
          inject: [ConfigService],
          useFactory: async (config: ConfigService) => ({
            uri: config.get('MONGO_URI'),
            useNewUrlParser: true,
            useUnifiedTopology: true,
            // useCreateIndex: true,
            // useFindAndModify: false,
          }),
        }),

        ApiMainSharedMongooseModule,
        ApiMainSharedCaslModule,
        ApiMainCloudinaryModule,
      ],

      controllers: [ApiMainAccountImageController],
      providers: [ApiMainAccountImageService],
    }).compile();

    controller = module.get<ApiMainAccountImageController>(
      ApiMainAccountImageController
    );
  });

  it('should be defined', () => {
    expect(controller).toBeDefined();
  });
});
