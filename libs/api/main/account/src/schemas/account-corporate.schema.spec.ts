import { AccountCorporate } from './account-corporate.schema';

describe('AccountCorporate Data-access', () => {
  it('should be defined', () => {
    expect(new AccountCorporate()).toBeDefined();
  });
});
