{
  "root": "apps/front-public",
  "sourceRoot": "apps/front-public",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/front-public",
        "outputPath": "dist/apps/front-public"
      },
      "configurations": {
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "options": {
        "buildTarget": "front-public:build",
        "dev": true
      },
      "configurations": {
        "production": {
          "buildTarget": "front-public:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "front-public:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/front-public"],
      "options": {
        "jestConfig": "apps/front-public/jest.config.js",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/front-public/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": ["scope:front-public", "type:app"]
}
